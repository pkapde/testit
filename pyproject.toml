[project]
name = "contractiq"
version = "0.1.0"
description = "Motor insurance claim document validation service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py311"
